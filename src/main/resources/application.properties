# 서버 포트 설정
server.port=8080

# 로깅 설정
logging.level.com.aihelper=DEBUG

# JWT 설정
jwt.secret=aihelper-jwt-secret-key-1234567890-abcdefghijklmnopqrstuvwxyz
jwt.expiration=86400000

# MyBatis 설정
mybatis.mapper-locations=classpath:mybatis/**/*.xml
mybatis.type-aliases-package=com.aihelper.model

# CORS 설정
# CORS 허용 도메인 (모든 도메인 허용)
cors.allowed-origins=*

# 순환 참조 허용 (임시 해결책)
spring.main.allow-circular-references=true

# 데이터베이스 자동 구성 비활성화 (1단계에서는 DB 연결 없이 진행)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration, org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# MySQL 설정 (나중에 사용)
# spring.datasource.url=jdbc:mysql://localhost:3306/aihelper
# spring.datasource.username=root
# spring.datasource.password=password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver 